n = float(input('Digite o valor em metros para converter para as outras medidas: '))
cm = n * 100
mm = n * 1000
dm = n * 10
dam = n / 10
hm = n / 100
km = n / 1000
print('{} metros é o mesmo que {}mm, {}cm, {}dm, {}dam, {}hm e {}km.'.format(n, mm,cm,dm, dam, hm, km))

