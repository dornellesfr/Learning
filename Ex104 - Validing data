def readInt(a):
    h = a
    while h != int:
        h = input('Type a number: ')
        if h.isnumeric():
            h = int(h)
            break
        else:
            print('Error: input a int number!')
    return h


n = readInt('Type a number: ')
print(f'You typed the number {n}')
